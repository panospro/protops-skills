---
description: Whole-repo pre-distribution audit + guided cleanup — personal identifiers, secrets, dangling gitignored refs, workflow/AI leakage, generated-file slop, dead config/code, PII. Stops with a fix-plan you select from before anything changes.
---

Run the `ship-ready` skill — its audit + guided-cleanup pipeline. Default target is all uncommitted changes (staged ∪ unstaged ∪ untracked); the arguments below set mode / scope / flags.

Arguments: $ARGUMENTS
